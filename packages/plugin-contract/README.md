# @lensx/plugin-contract

Public lensX plugin Manifest types, Schema, version constants, diagnostics, and
deterministic validation and normalization functions.

The package validates static author input only. It does not discover, install,
register, authorize, render, or execute plugins.
