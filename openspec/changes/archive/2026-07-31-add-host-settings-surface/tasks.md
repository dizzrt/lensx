## 1. Rust 偏好持久化

- [x] 1.1 定义可序列化、可验证的 `AppPreferences`、主题/语言枚举、默认值和稳定错误类型。
- [x] 1.2 实现应用配置目录中的偏好读取与原子写入，覆盖文件缺失、无效内容和 I/O 失败。
- [x] 1.3 暴露并注册类型化 Tauri 偏好读取/写入命令，确保命令边界再次验证输入。
- [x] 1.4 添加 Rust 单元测试，验证默认值、有效往返、无效枚举、损坏文件、原子替换与安全错误序列化。

## 2. 前端偏好引导

- [x] 2.1 添加与 Rust 载荷一致的 TypeScript 偏好类型、运行时校验和类型化桌面适配器。
- [x] 2.2 在产品 App 渲染前读取偏好，并用成功结果或安全默认值初始化现有主题与国际化 Provider。
- [x] 2.3 添加前端测试，验证已保存偏好恢复、文件缺失默认值、读取失败回退及本地化诊断状态。

## 3. 最小页面导航与 App Shell

- [x] 3.1 实现受信任 Host 页面目录、扁平 `ActivePage` 身份和与 React 无关的最小 `AppNavigationService`，包含目标预检及单一处理器生命周期。
- [x] 3.2 在 App Shell 本地状态中接入 `activePage`，从查询与活动页面推导 `home`、`search`、`page` 三种呈现状态。
- [x] 3.3 实现公共内容区域与页面上下文头部，确保进入页面时清理搜索状态、关闭时返回主页并恢复输入焦点。
- [x] 3.4 增加页面级错误边界，使进入后的页面故障保留头部、关闭按钮、当前主题与当前语言。
- [x] 3.5 添加 App Shell 与导航服务测试，覆盖三种状态、成功打开/关闭、预检失败保留查询、重复处理器保护、焦点恢复及页面内失败。

## 4. Host 设置能力

- [x] 4.1 注册 `lensx.core.open_settings` 的本地化 Action 元数据与受信任 executor，通过 `AppNavigationService` 打开固定 Host 设置页且不改变公共 Action 合约。
- [x] 4.2 使用现有 Semi Design、UnoCSS 与 Less 实现设置页面的“偏好”和“插件”两个一级部分，以及插件空占位。
- [x] 4.3 实现主题和语言控件的串行保存流程；仅在 Rust 确认成功后更新根级 Provider，并在失败时保留已确认值和显示本地化反馈。
- [x] 4.4 补齐英文与简体中文产品消息，保持消息键一致，并为设置 Action、页面头部、设置项、空占位和错误反馈提供文案。
- [x] 4.5 添加前端测试，覆盖 Action 搜索/执行、页面身份、单窗口呈现、主题/语言成功切换、保存失败、插件空占位、键盘交互与可访问名称。

## 5. 文档

- [x] 5.1 更新相关 `docs/en/` 架构与前端文档，记录 Host 设置归属、公共内容区域、最小页面导航和偏好持久化边界。
- [x] 5.2 同步更新相同相对路径的 `docs/zh/` 简体中文镜像，并确认中英文索引在新增文档时保持一致。

## 6. 最终校验

- [x] 6.1 运行 `pnpm run test`，修复本次变更引入的全部失败与警告后重新运行。
- [x] 6.2 运行 `pnpm run format` 和 `pnpm run check`，修复本次变更引入的全部格式、Lint 与静态检查问题后重新运行。
- [x] 6.3 运行 `pnpm run typecheck` 和 `pnpm run build`，修复本次变更引入的全部类型与构建问题后重新运行。
- [x] 6.4 运行 `pnpm run src-tauri:format` 与 `pnpm run src-tauri:format:check`，修复本次变更引入的全部 Rust 格式问题后重新运行。
- [x] 6.5 运行 `pnpm run src-tauri:test` 和 `pnpm run src-tauri:check`，修复本次变更引入的全部 Rust 测试、警告与静态检查问题后重新运行。
- [x] 6.6 运行严格 OpenSpec 校验；若任何最终校验曾失败，修复后重新运行失败命令以及 6.1–6.6 的完整校验集。

## 7. 运行态可见性修正

- [x] 7.1 更新 launcher window delta spec 与设计，定义 `home`、`search`、`page` 固定离散高度和受约束的 Rust 边界。
- [x] 7.2 实现 Rust 呈现模式命令、TypeScript 桌面适配器和 App Shell 三态高度同步，确保公共内容区可见。
- [x] 7.3 添加 Rust 与前端回归测试，并同步中英文架构文档中的窗口形状说明。
- [x] 7.4 重新运行 6.1–6.6 的完整验证集，修复本次修正引入的全部失败和警告。
