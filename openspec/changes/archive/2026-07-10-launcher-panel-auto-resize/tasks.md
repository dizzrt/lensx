## 1. 窗口与权限准备

- [x] 1.1 确认 Tauri window API 调整当前窗口尺寸所需的最小 capability 权限，并只为 `main` 窗口补充必要权限。
- [x] 1.2 调整主窗口初始尺寸配置，使启动器窗口宽度为 `650px`，高度可承载面板初始状态，同时保持用户不可手动缩放。
- [x] 1.3 保留现有全局快捷键、托盘、关闭隐藏和始终置顶行为，不引入无关桌面生命周期改动。

## 2. 前端状态与文案

- [x] 2.1 在启动器前端建立 `query` 状态、默认分区 mock 数据和匹配结果 mock 数据。
- [x] 2.2 将搜索占位、分区标题、辅助操作和条目展示所需用户文案接入应用 i18n messages。
- [x] 2.3 确认新增 Naive UI 组件仍位于现有 Provider、主题、locale 和 dateLocale 同步机制之下。

## 3. 面板布局与样式

- [x] 3.1 将 `App.vue` 从单行输入框改为一体化 launcher card，包含顶部搜索输入区和下方内容主体。
- [x] 3.2 实现默认状态，仅展示“最近使用”和“已固定”，不展示“市场精选”和头像栏。
- [x] 3.3 实现搜索状态，输入非空时仅展示“匹配结果”，清空输入后恢复默认状态。
- [x] 3.4 使用 UnoCSS 表达基础布局、间距和网格，使用 Less 表达卡片视觉、滚动条、状态样式和主题变量绑定。
- [x] 3.5 验证 light/dark 主题下背景、文本、边框、阴影和滚动区域均保持可读，避免亮色硬编码冲突。

## 4. 高度自适应与拖拽输入

- [x] 4.1 为内容主体设置自然高度、最大高度和内部 `overflow-y: auto`，确保搜索输入区不进入滚动区域。
- [x] 4.2 使用 `ResizeObserver` 监听 launcher card 高度，并对目标高度做最小值、`800px` 最大值和重复调用保护。
- [x] 4.3 调用 Tauri window API 根据测量高度调整当前主窗口尺寸，保持窗口宽度固定为 `650px`。
- [x] 4.4 实现顶部固定、向下展开或收起策略：调整窗口高度时不主动重新设置窗口位置。
- [x] 4.5 配置拖拽区域，使输入控件外的面板区域可拖拽窗口，输入控件本身可点击、输入和选择文本。

## 5. 验证

- [x] 5.1 运行 `pnpm run build` 验证前端生产构建。
- [x] 5.2 运行 `pnpm run check` 或等效 Biome 检查，处理格式和静态检查问题。
- [x] 5.3 运行 `pnpm run tauri dev` 做桌面烟测，验证输入、默认/搜索状态切换、拖拽、窗口自动高度和主体内部滚动。
- [x] 5.4 手动确认本变更未实现真实搜索、最近使用持久化、固定项持久化、市场精选或头像入口。
