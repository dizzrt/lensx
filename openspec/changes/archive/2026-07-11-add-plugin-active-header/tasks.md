## 1. 状态与数据推导

- [x] 1.1 在 `src/App.vue` 中从 `pluginNavigation.currentPageId` 推导当前插件页面和当前插件 manifest。
- [x] 1.2 新增插件关闭处理函数，关闭时将当前插件页面置空并清空搜索词。
- [x] 1.3 确认插件打开态只读取 `plugin.name`，不展示 `page.title`，且不扩展插件 manifest contract。

## 2. 顶部插件标题区

- [x] 2.1 将顶部区域改为搜索态和插件打开态互斥渲染：搜索态展示 `NInput`，插件打开态展示插件名称和关闭按钮。
- [x] 2.2 使用 Naive UI 组件实现关闭按钮，并为按钮接入应用级 i18n 可访问名称。
- [x] 2.3 使用 UnoCSS 处理标题区基础行内布局，使关闭按钮紧跟插件名称后方且不右对齐。
- [x] 2.4 使用 Less 补充标题区语义样式、长插件名称省略、关闭按钮可见性、hover/focus 状态和 light/dark 主题变量适配。
- [x] 2.5 确认标题区空白处仍可拖拽窗口，关闭按钮点击不触发窗口拖拽。
- [x] 2.6 确认插件导航状态变化后继续触发窗口高度自适应测量。

## 3. 验证

- [x] 3.1 运行 `pnpm run check` 验证格式和静态检查。
- [x] 3.2 运行 `pnpm run build` 验证前端生产构建。
- [x] 3.3 手动验证打开设置插件后顶部只显示插件名称和紧邻关闭按钮，不显示搜索框或当前页面名称。
- [x] 3.4 手动验证点击关闭按钮后回到工具主页、搜索词为空、顶部恢复搜索输入态。
- [x] 3.5 运行 `openspec status --change add-plugin-active-header` 确认变更 artifacts 和任务状态可追踪。
