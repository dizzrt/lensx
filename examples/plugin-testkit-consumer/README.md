# Plugin Testkit external consumer fixture

This isolated-consumer source is copied outside the workspace by the Testkit
pack gate. It is release validation input, not the official plugin project
template.
